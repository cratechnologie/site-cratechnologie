
  export interface SysUserRegistrationEditionDataRequest {

    sysUserRegistrationId  ? : string ;
    sysUserRegistrationSysTiersAutolinkGroupIds ? : string [] ;
    sysUserRegistrationTypeId ? : string ;

    sysTiersNonHumanId ? : string ;
    sysTiersPoolOwnerId ? : string ;
    delegated ? : number ;

  }
